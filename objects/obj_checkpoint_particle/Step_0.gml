size -= 0.01
image_xscale = size
image_yscale = size

if (size <= 0) instance_destroy();

if (float = true)
{
	x += lengthdir_x(spd,dir)
	y += lengthdir_y(spd,dir)
	d += 1
	d = min(99,d)
	
	depth = dd - d
}